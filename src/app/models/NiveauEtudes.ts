export interface NiveauEtudes {
  id?: number;        // Identifiant unique du niveau d'études (optionnel lors de la création)
  niveau: string;     // Nom du niveau d'études (ex. "Licence", "Master")
}
