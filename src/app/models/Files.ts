export interface Files {        // Identifiant unique du fichier (optionnel lors de la création)
  id?: number;
  url?: string;         // Nom du fichier
  type?: string;    // Identifiant de fichier
}
